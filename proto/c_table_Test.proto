import "common_scene.proto";

package Table;

message Test
{
	repeated sint32 id = 1; //ID
	repeated Common.SceneType name = 2 [default = SCENE_TYPE_INVALID]; //姓名
}

message Test_ARRAY
{
	repeated Test rows = 1;
}
